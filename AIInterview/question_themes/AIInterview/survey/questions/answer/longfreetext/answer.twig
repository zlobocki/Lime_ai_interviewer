{#
/**
 * AI Interview question type — answer template
 *
 * This template renders the standard Long Free Text textarea.
 * The AIInterview plugin's afterRenderQuestion event handler then
 * replaces this textarea with the AI chat widget.
 *
 * We intentionally render only the plain textarea here (not the widget)
 * to avoid dual-rendering issues. The PHP plugin handles all widget
 * injection via afterRenderQuestion.
 */
#}
<textarea
    name="{{ name }}"
    id="answer{{ name }}"
    class="ls-answers form-control ai-interview-placeholder"
    rows="5"
    aria-labelledby="ls-question-text-{{ basename }}"
>{{ dispVal }}</textarea>
